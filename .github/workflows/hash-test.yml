name: Hash Test

on:
  workflow_dispatch:

jobs:
  find_hash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo ${{ hashFiles('pack/pack.mcmeta') }}
        
